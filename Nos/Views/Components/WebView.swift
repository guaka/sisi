import SwiftUI

// Intentionally empty — external http(s) links open in SFSafariViewController via SafariPresenter.
// Kept so existing Xcode project references continue to compile if any previews import this file.
